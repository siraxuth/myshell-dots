input {
    kb_layout = us,th
    # us=default (index 0), th=secondary. Lock forces us; see keybinds.conf.
    kb_options =

    sensitivity = 0.3
    accel_profile = adaptive
}

# Force Intel iGPU primary to avoid NVIDIA/Intel dmabuf cross-GPU crash
# (card1 = Intel i915, card0 = NVIDIA)
env = AQ_DRM_DEVICES,/dev/dri/card1:/dev/dri/card0
env = WLR_DRM_DEVICES,/dev/dri/card1:/dev/dri/card0

exec-once = ~/.config/hypr/scripts/monitor-watcher.fish

# Super+Alt+M → open monitor layout editor (nwg-displays)
bind = $kbMonitorSetup, exec, nwg-displays

# ── Workspace mode (separate per-monitor / shared) ────────────────────────────
# Toggle it in Control Center → Display & Workspaces. The pane runs
# scripts/workspace-mode.fish which regenerates this sourced file + reloads Hyprland.
# "separate" binds ws 1-10 → HDMI-A-2, 11-20 → eDP-1; wsaction.fish's group-of-10 math
# then makes Super+1..0 act on the focused monitor. "shared" = Hyprland default.
source = $cConf/workspace-split.conf
